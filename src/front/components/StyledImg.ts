import { styled } from "@mui/material";
import type { ComponentProps } from "react";

export const StyledImg = styled("img")(() => ({}));

export type TStyledImgProps = ComponentProps<typeof StyledImg>;
